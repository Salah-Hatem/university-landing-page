PAYLOAD_SECRET=replace-with-long-random-secret
POSTGRES_URL=postgres://user:password@host:5432/university_landing
# Optional — media uploads go to Vercel Blob when set; falls back to local disk (media/) when unset.
BLOB_READ_WRITE_TOKEN=
